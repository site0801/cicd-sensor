rule_sets:
  - ruleset_id: cicd_sensor_baseline/generic_credential_access
    lists:
      package_manager_suffixes:
        - "/npm"
        - "/pnpm"
        - "/yarn"
        - "/bun"

      package_manager_argv_fragments:
        - "npm-cli.js"
        - "pnpm.cjs"
        - "yarn.js"
        - "install"
        - "ci"
        - "--foreground-scripts"

      python_suffixes:
        - "/python"
        - "/python3"
        - "/pip"
        - "/pip3"
        - "/poetry"

      action_tool_fragments:
        - "/_actions/"
        - "/actions-runner/"

      aws_credential_suffixes:
        - "/.aws"
        - "/.aws/credentials"
        - "/.aws/config"

      gcp_credential_suffixes:
        - "/.config/gcloud"
        - "/.config/gcloud/application_default_credentials.json"
        - "/.config/gcloud/credentials.db"

      azure_credential_suffixes:
        - "/.azure"
        - "/.azure/accessTokens.json"

      vault_credential_suffixes:
        - "/.vault-token"
        - "/run/secrets/vault_token"

      npm_credential_suffixes:
        - "/.npmrc"

      pypi_credential_suffixes:
        - "/.pypirc"

      docker_credential_suffixes:
        - "/.docker"
        - "/.docker/config.json"

      kubernetes_credential_suffixes:
        - "/.kube"
        - "/.kube/config"
        - "/serviceaccount/token"

      github_credential_suffixes:
        - "/.config/gh"
        - "/.config/gh/hosts.yml"

      git_credential_suffixes:
        - "/.git-credentials"
        - "/.netrc"

      ssh_key_suffixes:
        - "/.ssh/id_rsa"
        - "/.ssh/id_ed25519"
        - "/.ssh/id_ecdsa"
        - "/.ssh/id_dsa"

      gpg_key_suffixes:
        - "/.gnupg/secring.gpg"
        - "/.gnupg/private-keys-v1.d"

      ai_devtool_fragments:
        - "/.claude"
        - "/.cursor"
        - "/.codex"
        - "/.config/codex"
        - "/.kiro"
        - "/.continue"

      env_secret_suffixes:
        - "/.env"

      shell_history_suffixes:
        - "/.bash_history"
        - "/.zsh_history"
        - "/.sh_history"

      credential_suffixes:
        - "/.aws"
        - "/.aws/credentials"
        - "/.aws/config"
        - "/.config/gcloud"
        - "/.config/gcloud/application_default_credentials.json"
        - "/.config/gcloud/credentials.db"
        - "/.azure"
        - "/.azure/accessTokens.json"
        - "/.vault-token"
        - "/run/secrets/vault_token"
        - "/.npmrc"
        - "/.pypirc"
        - "/.docker"
        - "/.docker/config.json"
        - "/.kube"
        - "/.kube/config"
        - "/serviceaccount/token"
        - "/.config/gh"
        - "/.config/gh/hosts.yml"
        - "/.git-credentials"
        - "/.netrc"
        - "/.ssh/id_rsa"
        - "/.ssh/id_ed25519"
        - "/.ssh/id_ecdsa"
        - "/.ssh/id_dsa"
        - "/.gnupg/secring.gpg"
        - "/.gnupg/private-keys-v1.d"
        - "/.env"
        - "/.bash_history"
        - "/.zsh_history"
        - "/.sh_history"

      credential_fragments:
        - "/.claude"
        - "/.cursor"
        - "/.codex"
        - "/.config/codex"
        - "/.kiro"
        - "/.continue"

    rules:
      - rule_id: github_cli_token_exposure
        rule_name: "Supply-chain: GitHub CLI token exposure"
        description: |
          What:
            Collects commands that print a GitHub CLI token, such as gh auth token or gh auth status --show-token.
          Why:
            GitHub tokens are commonly used for repository exfiltration, workflow mutation, and propagation.
          Notes:
            Review process lineage, audit GitHub API activity, and rotate the token if this was not an approved release or diagnostic step.
        event_type: process_exec
        condition: |
          process.exec_path.endsWith("/gh") &&
          process.argv.exists(arg, arg == "auth") &&
          (
            process.argv.exists(arg, arg == "token") ||
            process.argv.exists(arg, arg == "--show-token")
          )
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: aws_credential_read
        rule_name: "Supply-chain: AWS credential surface read"
        description: |
          What:
            Collects reads or enumeration of AWS credential locations such as ~/.aws, ~/.aws/credentials, and ~/.aws/config.
          Why:
            Cloud credentials are common supply-chain harvest targets.
          Notes:
            Confirm whether this process normally needs AWS credentials.
        event_type: file_open
        condition: is_read && list.aws_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: gcp_credential_read
        rule_name: "Supply-chain: GCP credential surface read"
        description: |
          What:
            Collects reads or enumeration of Google Cloud SDK credential locations such as ~/.config/gcloud and ADC files.
          Why:
            Broad cloud credential discovery is common after malicious package or developer-tool execution.
          Notes:
            Check the ancestor process and verify whether GCP access is expected for this job.
        event_type: file_open
        condition: is_read && list.gcp_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: azure_credential_read
        rule_name: "Supply-chain: Azure credential surface read"
        description: |
          What:
            Collects reads or enumeration of Azure CLI credential locations such as ~/.azure and accessTokens.json.
          Why:
            CI and developer environments may hold reusable cloud tokens.
          Notes:
            Inspect ancestry and confirm Azure token cache access is expected in this workflow.
        event_type: file_open
        condition: is_read && list.azure_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: vault_credential_read
        rule_name: "Supply-chain: Vault credential surface read"
        description: |
          What:
            Collects reads of Vault token locations such as ~/.vault-token and /run/secrets/vault_token.
          Why:
            Secret-manager tokens can unlock additional credentials.
          Notes:
            Verify whether the process normally uses Vault and rotate exposed tokens if access is unexpected.
        event_type: file_open
        condition: is_read && list.vault_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: npm_credential_read
        rule_name: "Supply-chain: npm credential surface read"
        description: |
          What:
            Collects reads of .npmrc files that may contain npm registry tokens.
          Why:
            Stolen npm tokens can publish follow-on malicious package versions.
          Notes:
            Check whether this is a legitimate package manager operation or an unexpected descendant of install-time code.
        event_type: file_open
        condition: is_read && list.npm_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: pypi_credential_read
        rule_name: "Supply-chain: PyPI credential surface read"
        description: |
          What:
            Collects reads of .pypirc files that may contain Python package registry credentials.
          Why:
            Compromised publisher tokens can turn local execution into registry propagation.
          Notes:
            Confirm whether this job should publish Python packages and review nearby process and network activity.
        event_type: file_open
        condition: is_read && list.pypi_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: docker_credential_read
        rule_name: "Supply-chain: Docker credential surface read"
        description: |
          What:
            Collects reads or enumeration of Docker registry credential locations such as ~/.docker and config.json.
          Why:
            Registry credentials can pull private images or publish malicious images.
          Notes:
            Review lineage and whether image registry access is expected at this point in the job.
        event_type: file_open
        condition: is_read && list.docker_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: kubernetes_credential_read
        rule_name: "Supply-chain: Kubernetes credential surface read"
        description: |
          What:
            Collects reads or enumeration of Kubernetes access material such as ~/.kube, kubeconfig, and service account tokens.
          Why:
            These credentials can allow cluster discovery or lateral movement.
          Notes:
            Check whether the process should access Kubernetes and inspect package, Python, or action ancestry.
        event_type: file_open
        condition: is_read && list.kubernetes_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: github_credential_read
        rule_name: "Supply-chain: GitHub credential surface read"
        description: |
          What:
            Collects reads or enumeration of GitHub CLI credential locations such as ~/.config/gh and hosts.yml.
          Why:
            GitHub tokens can enable repository exfiltration, workflow mutation, and package propagation.
          Notes:
            Review process lineage and audit GitHub API activity if access is unexpected.
        event_type: file_open
        condition: is_read && list.github_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: git_credential_read
        rule_name: "Supply-chain: Git credential surface read"
        description: |
          What:
            Collects reads of Git HTTPS credential files such as .git-credentials and .netrc.
          Why:
            These credentials can provide repository access outside the current checkout.
          Notes:
            Confirm the process is a legitimate Git client and inspect later network or repository operations.
        event_type: file_open
        condition: is_read && list.git_credential_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: ssh_key_read
        rule_name: "Supply-chain: SSH private key read"
        description: |
          What:
            Collects reads of common SSH private key files.
          Why:
            Private keys can provide repository, server, or deployment access after compromised package or tool execution.
          Notes:
            Review whether the process should use SSH and rotate keys if access came from unexpected install-time or import-time code.
        event_type: file_open
        condition: is_read && list.ssh_key_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: gpg_key_read
        rule_name: "Supply-chain: GPG key surface read"
        description: |
          What:
            Collects reads of GPG secret keyring locations.
          Why:
            Signing material may be used to forge releases or decrypt protected data.
          Notes:
            Check whether the job normally signs artifacts and verify process ancestry before trusting the resulting build.
        event_type: file_open
        condition: is_read && list.gpg_key_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: ai_devtool_surface_read
        rule_name: "Supply-chain: AI developer-tool surface read"
        description: |
          What:
            Collects reads or enumeration of AI and developer-tool configuration surfaces such as Claude, Cursor, Codex, Kiro, and Continue settings.
          Why:
            Recent supply-chain payloads target editor and agent configuration for credentials and persistence.
          Notes:
            Review process lineage and inspect these files for unexpected modifications.
        event_type: file_open
        condition: is_read && list.ai_devtool_fragments.exists(p, path.contains(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: env_secret_read
        rule_name: "Supply-chain: env secret file read"
        description: |
          What:
            Collects reads of .env files.
          Why:
            Application environment files often hold API keys and service credentials useful to supply-chain payloads.
          Notes:
            Confirm whether the process normally reads project secrets and inspect unexpected package or tool ancestry.
        event_type: file_open
        condition: is_read && list.env_secret_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: proc_environ_read
        rule_name: "Supply-chain: process environment read"
        description: |
          What:
            Collects reads of /proc/<pid>/environ.
          Why:
            CI secrets and short-lived tokens are commonly exposed through process environments.
          Notes:
            Review the reader and target process, then rotate exposed credentials if access was unexpected.
        event_type: file_open
        condition: is_read && path.contains("/proc/") && path.endsWith("/environ")
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: shell_history_read
        rule_name: "Supply-chain: shell history read"
        description: |
          What:
            Collects reads of shell history files.
          Why:
            CI jobs rarely need interactive shell history, and history can contain pasted tokens or secret-bearing CLI usage.
          Notes:
            Treat unexpected shell history access as credential discovery.
        event_type: file_open
        condition: is_read && list.shell_history_suffixes.exists(p, path.endsWith(p))
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: anchored_credential_read
        rule_name: "Supply-chain: credential read under package, Python, or action ancestry"
        description: |
          What:
            Collects sensitive credential reads below package install, Python startup/import, or action execution ancestry.
          Why:
            Credential access is more suspicious when it is a side effect of dependency installation or dynamic developer tooling.
          Notes:
            Review the matched ancestor and decide whether that package, import, or action should access the credential family.
        event_type: file_open
        condition: |
          is_read &&
          (
            list.credential_suffixes.exists(p, path.endsWith(p)) ||
            list.credential_fragments.exists(p, path.contains(p)) ||
            (path.contains("/proc/") && path.endsWith("/environ"))
          ) &&
          process.ancestors.exists(a,
            (
              list.package_manager_suffixes.exists(p, a.exec_path.endsWith(p)) &&
              a.argv.exists(arg, arg == "install" || arg == "ci")
            ) ||
            (
              a.exec_path.endsWith("/node") &&
              a.argv.exists(arg, list.package_manager_argv_fragments.exists(p, arg.contains(p)))
            ) ||
            list.python_suffixes.exists(p, a.exec_path.endsWith(p)) ||
            list.action_tool_fragments.exists(p, a.exec_path.contains(p))
          )
        action: collect
        tags:
          mitre_tactic: credential-access

      - rule_id: proc_maps_read
        rule_name: "Supply-chain: process memory map read"
        description: |
          What:
            Collects reads of /proc/<pid>/maps.
          Why:
            Memory scraping often reads maps before dumping process memory, but maps alone can occur during normal runtime startup.
          Notes:
            Use this with the maps-and-mem correlation.
        event_type: file_open
        condition: is_read && path.contains("/proc/") && path.endsWith("/maps")
        action: collect
        max_alerts: 1
        tags:
          mitre_tactic: credential-access

      - rule_id: proc_mem_read
        rule_name: "Supply-chain: process memory read"
        description: |
          What:
            Collects reads of /proc/<pid>/mem.
          Why:
            Direct process memory access can expose CI secrets, tokens, and runtime credentials.
          Notes:
            Treat access combined with /proc/<pid>/maps as likely memory scraping.
        event_type: file_open
        condition: is_read && path.contains("/proc/") && path.endsWith("/mem")
        action: collect
        max_alerts: 1
        tags:
          mitre_tactic: credential-access

      - rule_id: multi_family_credential_access
        rule_name: "Supply-chain: credential surface enumeration"
        description: |
          What:
            Detects a job that touches five or more distinct credential families: AWS, GCP, Azure, Vault, npm, PyPI, Docker, Kubernetes, GitHub, Git, SSH, GPG, AI/devtool, .env, shell history, or process environment.
          Why:
            Broad cross-family enumeration is unusual for normal tooling and matches credential-harvesting payload behavior.
          Notes:
            Review family hits, identify common lineage, and rotate exposed credentials.
        type: correlation
        condition: |
          (
            (rule.aws_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.gcp_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.azure_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.vault_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.npm_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.pypi_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.docker_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.kubernetes_credential_read.total_count >= 1 ? 1 : 0) +
            (
              (
                rule.github_credential_read.total_count >= 1 ||
                rule.github_cli_token_exposure.total_count >= 1
              ) ? 1 : 0
            ) +
            (rule.git_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.ssh_key_read.total_count >= 1 ? 1 : 0) +
            (rule.gpg_key_read.total_count >= 1 ? 1 : 0) +
            (rule.ai_devtool_surface_read.total_count >= 1 ? 1 : 0) +
            (rule.env_secret_read.total_count >= 1 ? 1 : 0) +
            (rule.shell_history_read.total_count >= 1 ? 1 : 0) +
            (rule.proc_environ_read.total_count >= 1 ? 1 : 0)
          ) >= 5
        action: detect
        tags:
          mitre_tactic: credential-access

      - rule_id: anchored_multi_family_credential_access
        rule_name: "Supply-chain: anchored credential surface enumeration"
        description: |
          What:
            Detects three or more distinct credential families from the same family set when at least one credential read happened below package install, Python startup/import, or GitHub Actions ancestry.
          Why:
            Supply-chain payloads often run through those entry points before sweeping available credentials.
          Notes:
            Review the anchor process, source package or action, and subsequent egress or API activity.
        type: correlation
        condition: |
          rule.anchored_credential_read.total_count >= 1 &&
          (
            (rule.aws_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.gcp_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.azure_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.vault_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.npm_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.pypi_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.docker_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.kubernetes_credential_read.total_count >= 1 ? 1 : 0) +
            (
              (
                rule.github_credential_read.total_count >= 1 ||
                rule.github_cli_token_exposure.total_count >= 1
              ) ? 1 : 0
            ) +
            (rule.git_credential_read.total_count >= 1 ? 1 : 0) +
            (rule.ssh_key_read.total_count >= 1 ? 1 : 0) +
            (rule.gpg_key_read.total_count >= 1 ? 1 : 0) +
            (rule.ai_devtool_surface_read.total_count >= 1 ? 1 : 0) +
            (rule.env_secret_read.total_count >= 1 ? 1 : 0) +
            (rule.shell_history_read.total_count >= 1 ? 1 : 0) +
            (rule.proc_environ_read.total_count >= 1 ? 1 : 0)
          ) >= 3
        action: detect
        tags:
          mitre_tactic: credential-access

      - rule_id: proc_maps_mem_combo
        rule_name: "Supply-chain: process maps and memory read"
        description: |
          What:
            Detects a job that reads both /proc/<pid>/maps and /proc/<pid>/mem.
          Why:
            maps can locate readable regions before dumping memory for CI secrets.
          Notes:
            Review reader and target processes, then rotate credentials that may have been resident in memory.
        type: correlation
        condition: |
          rule.proc_maps_read.total_count >= 1 &&
          rule.proc_mem_read.total_count >= 1
        action: detect
        tags:
          mitre_tactic: credential-access
