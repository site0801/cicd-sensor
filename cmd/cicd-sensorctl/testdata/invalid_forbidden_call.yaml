rule_sets:
  - ruleset_id: set-invalid-forbidden
    rules:
      - rule_id: no_timestamp
        event_type: network_connect
        condition: timestamp() == "2026-01-01T00:00:00Z"
        action: detect
